When updating the project
- Always check if the changes require an update to `trickle/notes/README.md`.
- If new features are added, update the "Features" section.
- If the file structure changes significantly, update the "Project Structure" section.